# Pocket Server

A Flutter-based Android manager that runs local Minecraft Java servers using aarch64 JDK, tunnels them via Playit.gg, and uses Google Drive for "Floating Host" synchronization (allowing different users to host the same world).

